# Simple Mandelbrot/Julia Set Renderer

## install
if needed:
```zsh
 npx tsc --init
```

install dependencies:
```zsh
 npm install canvas
```
```zsh
 npm install --save-dev @types/node
```

## compile & start
compile:
```zsh
 npx tsc
```
start:
```zsh
 node dist/mandelbrot.js
```
